{
  "name": "theia-glsp-debug-adapter",
  "keywords": [
    "theia-extension"
  ],
  "version": "0.1.0",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "vscode-debugadapter": "^1.32.0",
    "await-notify": "^1.0.1",
    "@types/vscode": "^1.34.0"
  },
  "devDependencies": {
    "rimraf": "^2.6.1",
    "tslint": "^5.5.0",
    "typescript": "3.4.3"
  },
  "scripts": {
    "prepare": "yarn run clean && yarn run build",
    "clean": "rimraf lib",
    "build": "tsc && yarn run lint",
    "lint": "tslint -c ../../configs/tslint.json --project ./tsconfig.json",
    "watch": "tsc -w"
  }
}
